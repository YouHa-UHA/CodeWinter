# Manager Brief 管理简报

本文用于记录当前项目的动态简报。

说明：
1. 当前 `CodeWinter` 本体仓中的这份文件是模板基线。
2. 真正接入项目后，应由管理线程改写为该实例的真实动态。

## 当前阶段
1. 状态：
   - Bootstrap 待启动
2. 当前管理租约：
   - 未领取
3. 当前本体发布基线：
   - `v0.1.1 / CodeWinter v0.1.x Harness Upgrade / Carrot on a Stick 胡萝卜钓竿`

## 当前控制目标
1. 完成 Bootstrap v1。
2. 建立第一版已验证工作区地图。
3. 识别第一批边界清晰的执行线程任务。

## 当前编排策略
1. 当前阶段优先分析和收敛，不先批量派发线程。
2. 只有 `Green` 任务默认允许“短分析后实现”。
3. `Yellow` 任务默认先输出约束、风险与协作需求，再决定是否实现。
4. `Red` 任务默认停在分析与确认阶段，不直接推进大规模改动。

## 工作区摘要
1. 项目类型：
   - To confirm
2. 仓库形态：
   - To confirm
3. 主要语言与框架：
   - To confirm
4. 主要应用或服务：
   - To confirm

## 当前风险
1. 业务事实尚未确认。
2. 启动、构建、测试入口可能仍未验证。
3. 服务或模块边界可能要等工作区扫描后才能收敛。

## 管理介入条件
1. 任一线程进入 `Red` 控制门。
2. 任一线程显式标记 `deviation_flag = true`。
3. 两个线程的 `scope_claims` 发生明显冲突。
4. 协作请求长期悬而未决，已影响推进节奏。
5. 线程低信心推进高影响任务。

## 当前收口条件
1. 线程已产出可集成的正式输出草稿。
2. 线程已准备好 handoff，且接力条件清晰。
3. 任务结果已进入“待归档确认”状态。
4. 当前阶段应先暂停扩散，转为汇总、确认或归档。

## 协作运行态摘要
1. 线程协作总览：
   - 查看 `./CodeWinter/00-control-plane/thread-board.md`
2. 线程状态卡：
   - 查看 `./CodeWinter/00-control-plane/threads/`
3. 协作请求池：
   - 查看 `./CodeWinter/00-control-plane/collab-requests/`

## 管理提示
1. 这里只写已确认事实。
2. 未知项必须显式保留。
3. 长期稳定事实后续应转写到服务、链路、证据层、决策层。
4. 运行态协作信息应优先写入线程卡、请求卡和总览页，不要直接写入长期知识层。
5. 管理线程不仅负责记录状态，还负责维持控制目标、编排策略和纠偏节奏。
