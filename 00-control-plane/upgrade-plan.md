# CodeWinter Upgrade Plan 升级计划

本文用于记录当前实例正在准备或执行中的升级计划。

如果当前没有升级进行中，保持最小占位即可，不写流水讨论。

说明：
1. 当前 `CodeWinter` 本体仓中的这份文件是模板基线。
2. 只有某个真实实例准备升级时，才应写入具体计划。

## 当前状态
1. `active_upgrade`: `none`
2. `requested_by`: `none`
3. `from_release_version`: `to confirm`
4. `to_release_version`: `to confirm`
5. `from_instance_schema_version`: `to confirm`
6. `to_instance_schema_version`: `to confirm`
7. `from_runtime_coordination_version`: `to confirm`
8. `to_runtime_coordination_version`: `to confirm`
9. `upgrade_type`: `to confirm`
10. `freeze_new_dispatch`: `no`
11. `compatibility_window_needed`: `to confirm`
12. `status`: `idle`

## 升级计划模板
1. 升级目标
2. 影响范围
3. 版本比较结果
4. 受影响目录
5. 可自动更新项
6. 必须人工确认项
7. 动态状态保护方式
8. 兼容期策略
9. 回滚思路
