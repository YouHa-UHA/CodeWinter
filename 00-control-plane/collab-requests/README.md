# Collaboration Requests 协作请求池

本目录用于存放线程发起的协作请求。

规则：
1. 一次协作请求一个文件
2. 发起线程优先只写自己创建的请求卡
3. 管理线程根据请求卡决定是否路由、转发、收口

推荐命名：
1. `collab-requests/<request-id>.md`

推荐 `request_id` 格式：
1. `<date>-<from-thread>-<type>-<seq>`
